images = {
  "community-ubuntu-server" = {
    core = {
      name        = "u2204s-dc"
      description = "Docker Community on Ubuntu Server"

      import_directory = "ubuntu-server"
      import_image     = "u2204s"
    }
  }
}
