driver:
  name: vagrant
  vm_hostname: false
  vagrantfiles:
    - ./kitchen.Vagrantfile
  box_url: <%= ENV['KITCHEN_VAGRANT_BOX_URL'] %>

verifier:
  name: inspec

platforms:
  - name: ubuntu

suites:
  - name: default
