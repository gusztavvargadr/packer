{
  "description": "{{user `description`}}",
  "builders": [
  ],
  "provisioners": [
    {
      "type": "file",
      "source": "disk/",
      "destination": "{{user `upload_directory`}}"
    },
  ],
  "post-processors": [
  ]
}
