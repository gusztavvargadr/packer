{
  "post-processors": [
    {
      "type": "vagrant",
      "keep_input_artifact": true,
      "output": "{{user `output_directory`}}/vagrant.box",
      "vagrantfile_template": "host/Vagrantfile"
    }
  ]
}
