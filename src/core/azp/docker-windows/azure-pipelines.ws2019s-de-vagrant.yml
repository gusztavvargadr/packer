trigger: none
pr: none

stages:
- stage: build
  displayName: Build

  jobs:
  - template: ../azure-pipelines.vagrant-build.yml
    parameters:
      images:
      - name:
        - ws2019s
        - de
      providers:
      - virtualbox
      - hyperv
