trigger: none
pr: none

resources:
  pipelines:
    - pipeline: parent
      source: w101909e-hyperv-core
      trigger:
        branches:
          include:
            - master

stages:
  - template: ../../ci-stages.yml
    parameters:
      image: w101909e
      provider: hyperv
      configuration: vagrant
      parent: w101909e

  - template: ../../cd-stages.yml
    parameters:
      image: w101909e
      provider: hyperv
      configuration: vagrant
