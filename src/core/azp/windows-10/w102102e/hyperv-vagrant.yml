trigger: none
pr: none

resources:
  pipelines:
    - pipeline: parent
      source: w102102e-hyperv-core
      trigger:
        branches:
          include:
            - master

stages:
  - template: ../../ci-stages.yml
    parameters:
      image: w102102e
      provider: hyperv
      configuration: vagrant
      parent: w102102e

  - template: ../../cd-stages.yml
    parameters:
      image: w102102e
      provider: hyperv
      configuration: vagrant
