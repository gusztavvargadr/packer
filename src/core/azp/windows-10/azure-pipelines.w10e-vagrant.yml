trigger: none
pr: none

stages:
- stage: build
  displayName: Build

  jobs:
  - template: ../azure-pipelines.vagrant-build.yml
    parameters:
      images:
      - name: w10e
      providers:
      - virtualbox
      - hyperv
