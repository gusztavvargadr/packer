trigger: none
pr: none

resources:
  pipelines:
    - pipeline: parent
      source: w1021h2e-dc-hyperv-core
      trigger:
        branches:
          include:
            - master

stages:
  - template: ../../ci-stages.yml
    parameters:
      image: w1021h2e-dc
      provider: hyperv
      configuration: vagrant
      parent: w1021h2e-dc

  - template: ../../cd-stages.yml
    parameters:
      image: w1021h2e-dc
      provider: hyperv
      configuration: vagrant
