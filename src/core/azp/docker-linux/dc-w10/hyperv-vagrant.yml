trigger: none
pr: none

resources:
  pipelines:
    - pipeline: parent
      source: dc-w10-hyperv-core
      trigger:
        branches:
          include:
            - master

stages:
  - template: ../../ci-stages.yml
    parameters:
      image: w102102e-dc
      provider: hyperv
      configuration: vagrant
      parent: w102102e-dc

  - template: ../../cd-stages.yml
    parameters:
      image: w102102e-dc
      provider: hyperv
      configuration: vagrant
