trigger: none
pr: none

stages:
- stage: build
  displayName: Build

  jobs:
  - template: ../vagrant-build.yml
    parameters:
      images:
      - name:
        - u16d
        - dc
      providers:
      - hyperv
