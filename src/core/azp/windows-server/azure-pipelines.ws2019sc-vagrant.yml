trigger: none
pr: none

stages:
- stage: build
  displayName: Build
  variables:
  - group: artifacts-localnetwork

  jobs:
  - template: ../azure-pipelines.vagrant-build.yml
    parameters:
      images:
      - name: ws2019sc
      providers:
      - virtualbox
      - hyperv
