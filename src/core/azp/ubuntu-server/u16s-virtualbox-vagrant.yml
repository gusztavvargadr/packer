trigger: none
pr: none

stages:
- stage: build
  displayName: Build

  jobs:
  - template: ../vagrant-build.yml
    parameters:
      images:
      - name: u16s
      providers:
      - virtualbox

- stage: deploy
  displayName: Deploy

  jobs:
  - template: ../vagrant-deploy.yml
    parameters:
      images:
      - name: u16s
      providers:
      - virtualbox
