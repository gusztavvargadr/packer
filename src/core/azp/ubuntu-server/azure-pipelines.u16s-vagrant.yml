trigger: none
pr: none

stages:
- stage: build
  displayName: Build

  jobs:
  - template: ../azure-pipelines.vagrant-build.yml
    parameters:
      images:
      - name: u16s
      providers:
      - virtualbox
      - hyperv
