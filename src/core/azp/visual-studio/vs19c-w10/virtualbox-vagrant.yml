trigger: none
pr: none

resources:
  pipelines:
    - pipeline: parent
      source: vs19c-w10-virtualbox-core
      trigger:
        branches:
          include:
            - master

stages:
  - template: ../../ci-stages.yml
    parameters:
      image: w102102e-dc-vs19c
      provider: virtualbox
      configuration: vagrant
      parent: w102102e-dc-vs19c

  - template: ../../cd-stages.yml
    parameters:
      image: w102102e-dc-vs19c
      provider: virtualbox
      configuration: vagrant
