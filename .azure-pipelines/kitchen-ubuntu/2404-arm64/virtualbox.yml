trigger: none
pr: none

resources:
  pipelines:
    - pipeline: source
      source: ubuntu-server.2404-lts-arm64.virtualbox

stages:
  - template: ../../stages.yml
    parameters:
      sample: kitchen-ubuntu
      image: 2404-arm64
      provider: virtualbox
      hostArchitecture: ARM64
