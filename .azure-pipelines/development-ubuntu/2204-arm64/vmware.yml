trigger: none
pr: none

resources:
  pipelines:
    - pipeline: source
      source: ubuntu-desktop.2204-lts-ubuntu-arm64.vmware

stages:
  - template: ../../stages.yml
    parameters:
      sample: development-ubuntu
      image: 2204-arm64
      provider: vmware
      hostArchitecture: ARM64
