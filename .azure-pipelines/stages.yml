parameters:
  sample: ''
  image: ''
  provider: ''
  hostArchitecture: ''
  artifactHandoff: conventional

stages:
  - stage: native
    displayName: Native

    jobs:
      - template: ./jobs.yml
        parameters:
          sample: ${{ parameters.sample }}
          image: ${{ parameters.image }}
          provider: ${{ parameters.provider }}
          hostArchitecture: ${{ parameters.hostArchitecture }}
          artifactHandoff: ${{ parameters.artifactHandoff }}
          build: native

  - stage: vagrant
    displayName: Vagrant

    jobs:
      - template: ./jobs.yml
        parameters:
          sample: ${{ parameters.sample }}
          image: ${{ parameters.image }}
          provider: ${{ parameters.provider }}
          hostArchitecture: ${{ parameters.hostArchitecture }}
          artifactHandoff: ${{ parameters.artifactHandoff }}
          build: vagrant
