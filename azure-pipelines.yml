trigger:
- master
- feature/*
- hotfix/*
- refs/tags/*

pr: none

stages:
- stage: ci
  displayName: CI

  jobs:
  - job: build
    displayName: Build
    pool:
      name: default

    steps:
    - powershell: |
        ci.ps1 info
      displayName: Diagnostics
